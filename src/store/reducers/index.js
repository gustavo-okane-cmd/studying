import ativosSlice from "./ativosSlice";
import autocompleteSlice from "./autocompleteSlice";

const reducer = {
    ativos: ativosSlice,
    autocomplete: autocompleteSlice,
};

export default reducer;
